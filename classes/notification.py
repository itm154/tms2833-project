from datetime import datetime

from .user import User


class Notification:
    __notificationID = 1

    def __init__(self, type: str, message: str, receiver: User):
        self.__notificationID = __notificationID  # Auto generated by system every time a notification is created
        self.__type = type  # What type of notification is this, can be removed
        self.__message = message  # The main box of the message
        self.__generatedDate = datetime.now()  # Generated date is the current time
        self.__isRead = False  # Notifications come as unread by default
        self.__receiver = (
            receiver  # Pass in the User object to obtain the name of the user
        )

        __notificationID = (
            __notificationID + 1
        )  # Increment ID for next notification after creation

    def markAsRead(self):
        self.__isRead = True
        print("The notification has been marked as read.")
        pass

    def display(self):
        print(self.__message)
        pass

    def sendTo(self, User):
        pass
